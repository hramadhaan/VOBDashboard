class User {
  constructor(uid, photoURL, email, displayName, typeUser) {
    this.uid = uid;
    this.photoURL = photoURL;
    this.email = email;
    this.displayName = displayName;
    this.typeUser = typeUser;
  }
}

export default User;
